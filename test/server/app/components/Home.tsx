import { h, Fragment, VNode } from 'preact'
import { Cap } from '../../../../src'

export const Home = (): VNode => (
  <Fragment>
    <Cap>
      <title>Home</title>
    </Cap>
    <div>This is Home</div>
  </Fragment>
)
